sqlite3 flatseeker.db

CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, username TEXT NOT NULL, hash TEXT NOT NULL, email TEXT NOT NULL);
CREATE UNIQUE INDEX username ON users (username);
CREATE TABLE 'flats' ('id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, 'title' TEXT, 'price' INTEGER NOT NULL, 'rooms' INTEGER NOT NULL, 'added_by' INTEGER NOT NULL, 'timestamp' DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, 'location' TEXT, 'link' TEXT, 'comments' TEXT);